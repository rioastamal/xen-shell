#!/bin/sh
#
#  Invoke screen with some sane options so that we can allow
# the user to connect to a Xen console safely.
#
# Steve
# --
# $Id: xen-login-shell,v 1.1 2006-07-28 22:38:17 steve Exp $


#
#  Options we use:
#
# -c file
#   Override the default configuration file from "$HOME/.screenrc" to file
#
# -d -R
#   Reattach a session and if necessary detach or even create it first.
#
# -s
#   set the login shell
#   
# -S
#   set the title of this screen
#

screen -d -R -c /usr/local/etc/_screenrc -e^Aa -s /usr/local/bin/xen-shell  -S $USER

